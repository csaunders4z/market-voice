version: '3.8'

services:
  market_voice:
    build: .
    volumes:
      - .:/app
      - ./logs:/app/logs
      - ./output:/app/output
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app